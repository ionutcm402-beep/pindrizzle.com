"use client";

import { useEffect, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import PingIcon from "@/components/PingIcon";
import { CATEGORY_DEFINITIONS, CATEGORY_ORDER, type PingCategoryKey, type Radius } from "@/lib/ping-categories";
import { readPingRadius, writePingRadius } from "@/lib/ping-local-preferences";
import { getPingLocationSilently, requestPingLocation, type PingCoordinates, type PingLocationState } from "@/lib/ping-location";

type CategoryKey = "all" | PingCategoryKey;
type SortKey = "best" | "newest" | "closest" | "confirmed";
type SearchRow = { id:string; user_id:string; category:PingCategoryKey; title:string; body:string; place_label:string|null; confirmation_count:number; comment_count:number; created_at:string; expires_at:string; distance_meters:number; search_rank:number; };
type SearchItem = SearchRow & { mediaUrl?:string; lastConfirmedAt?:string|null };
type PingMediaRow = { ping_id:string; storage_path:string };

type MetaRow = { id:string; last_confirmed_at:string|null };

function ageLabel(value:string){const m=Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/60000));if(m<1)return"just now";if(m<60)return`${m}m ago`;const h=Math.floor(m/60);return h<24?`${h}h ago`:`${Math.floor(h/24)}d ago`;}
function confirmLabel(value?:string|null){if(!value)return"";const m=Math.max(0,Math.floor((Date.now()-new Date(value).getTime())/60000));if(m<1)return"just now";if(m<60)return`${m}m ago`;const h=Math.floor(m/60);return h<24?`${h}h ago`:`${Math.floor(h/24)}d ago`;}

async function enrich(rows:SearchRow[]){if(!rows.length)return rows as SearchItem[];const supabase=createClient();const ids=rows.map(r=>r.id);const[media,meta]=await Promise.all([supabase.from("ping_media").select("ping_id,storage_path").in("ping_id",ids),supabase.from("pings").select("id,last_confirmed_at").in("id",ids)]);const urls=new Map<string,string>();if(!media.error&&media.data?.length){const mediaRows=media.data as PingMediaRow[];const signed=await supabase.storage.from("ping-media").createSignedUrls(mediaRows.map(r=>r.storage_path),900);if(!signed.error&&signed.data)mediaRows.forEach((r,i)=>{const url=signed.data?.[i]?.signedUrl;if(url)urls.set(r.ping_id,url);});}const last=new Map<string,string|null>();(meta.data||[]).forEach(r=>last.set(String((r as MetaRow).id),(r as MetaRow).last_confirmed_at));return rows.map(r=>({...r,mediaUrl:urls.get(r.id),lastConfirmedAt:last.get(r.id)||null}));}

export default function SearchPage(){
  const[query,setQuery]=useState("");const[debouncedQuery,setDebouncedQuery]=useState("");const[category,setCategory]=useState<CategoryKey>("all");const[radius,setRadius]=useState<Radius>(1);const[sort,setSort]=useState<SortKey>("best");const[coordinates,setCoordinates]=useState<PingCoordinates|null>(null);const[locationState,setLocationState]=useState<PingLocationState>("checking");const[items,setItems]=useState<SearchItem[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[refreshNonce,setRefreshNonce]=useState(0);
  useEffect(()=>{setRadius(readPingRadius());let cancelled=false;void getPingLocationSilently().then(result=>{if(cancelled)return;setLocationState(result.state);if(result.coordinates)setCoordinates(result.coordinates);});return()=>{cancelled=true;};},[]);
  useEffect(()=>{const timer=window.setTimeout(()=>setDebouncedQuery(query.trim()),260);return()=>window.clearTimeout(timer);},[query]);
  const requestLocation=async()=>{setLocationState("requesting");const result=await requestPingLocation();setLocationState(result.state);if(result.coordinates)setCoordinates(result.coordinates);};
  useEffect(()=>{if(!coordinates)return;let cancelled=false;const load=async()=>{setLoading(true);setError("");try{const{data,error:rpcError}=await createClient().rpc("search_nearby_pings",{viewer_lat:coordinates.lat,viewer_lng:coordinates.lng,search_query:debouncedQuery,category_filter:category==="all"?null:category,radius_meters:Math.round(radius*1609.344),result_limit:60});if(rpcError)throw rpcError;const next=await enrich((data||[])as SearchRow[]);if(!cancelled)setItems(next);}catch(e){console.error("Search failed",e);if(!cancelled){setItems([]);setError("Nearby search is temporarily unavailable.");}}finally{if(!cancelled)setLoading(false);}};void load();return()=>{cancelled=true;};},[coordinates,debouncedQuery,category,radius,refreshNonce]);
  useEffect(()=>{if(!coordinates)return;const supabase=createClient();const channel=supabase.channel("search-live-v3").on("postgres_changes",{event:"*",schema:"public",table:"pings"},()=>setRefreshNonce(v=>v+1)).subscribe();return()=>{void supabase.removeChannel(channel);};},[coordinates]);
  const sorted=useMemo(()=>{const next=[...items];if(sort==="newest")next.sort((a,b)=>new Date(b.created_at).getTime()-new Date(a.created_at).getTime());if(sort==="closest")next.sort((a,b)=>a.distance_meters-b.distance_meters);if(sort==="confirmed")next.sort((a,b)=>b.confirmation_count-a.confirmation_count||new Date(b.created_at).getTime()-new Date(a.created_at).getTime());if(sort==="best"&&debouncedQuery)next.sort((a,b)=>b.search_rank-a.search_rank||b.confirmation_count-a.confirmation_count);return next;},[items,sort,debouncedQuery]);
  const setAndStoreRadius=(next:Radius)=>{setRadius(next);writePingRadius(next);};
  const openPing=(item:SearchItem)=>{const meta=CATEGORY_DEFINITIONS[item.category];window.dispatchEvent(new CustomEvent("ping:open-detail",{detail:{id:item.id,title:item.title,body:item.body,category:meta.label,place:item.place_label||"Nearby",distanceMiles:item.distance_meters/1609.344,confirmations:item.confirmation_count,live:true}}));};
  const discoverMode=!debouncedQuery;
  return <div className="search-v3-page"><div className="search-v3-shell">
    <header className="search-v3-head"><button type="button" onClick={()=>window.location.assign("/")} aria-label="Back to Feed">‹</button><div><div className="brand small">ping<span>.</span></div><p>Search & discover</p></div><button type="button" onClick={()=>window.location.assign("/map")} aria-label="Open Map"><PingIcon name="map" size={18}/></button></header>
    <section className="search-v3-intro"><h1>{discoverMode?"Find what matters nearby":"Search your area"}</h1><p>{discoverMode?"Deals, parking, outages, traffic and useful local Pings — never a global feed.":`Active matches inside ${radius} ${radius===1?"mile":"miles"}.`}</p></section>
    <div className="search-v3-searchbox"><PingIcon name="lostFound" size={19}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try “road closed”, “parking”, “deal”…" maxLength={120} aria-label="Search nearby Pings"/>{query&&<button type="button" onClick={()=>setQuery("")} aria-label="Clear search">×</button>}</div>
    <div className="search-v3-chips" aria-label="Search categories"><button type="button" className={category==="all"?"active":""} onClick={()=>setCategory("all")}>All</button>{CATEGORY_ORDER.map(key=>{const meta=CATEGORY_DEFINITIONS[key];return <button key={key} type="button" className={category===key?"active":""} onClick={()=>setCategory(key)}><PingIcon name={meta.icon} size={14}/>{meta.shortLabel}</button>;})}</div>
    <div className="search-v3-controls"><label><span>Radius</span><select value={radius} onChange={e=>setAndStoreRadius(Number(e.target.value)as Radius)}><option value={0.5}>0.5 mi</option><option value={1}>1 mi</option><option value={3}>3 mi</option><option value={5}>5 mi</option></select></label><label><span>Sort</span><select value={sort} onChange={e=>setSort(e.target.value as SortKey)}><option value="best">{debouncedQuery?"Best match":"Recommended"}</option><option value="newest">Newest</option><option value="closest">Closest</option><option value="confirmed">Most confirmed</option></select></label></div>
    {locationState!=="granted"&&<section className="search-v3-location"><span><PingIcon name="location" size={20}/></span><div><strong>{locationState==="denied"?"Location is blocked":"Search starts with your location"}</strong><p>{locationState==="denied"?"Enable location in your browser settings, then try again.":"Search reuses the same Ping-wide permission as Feed and Map."}</p></div><button type="button" onClick={()=>void requestLocation()} disabled={locationState==="requesting"||locationState==="checking"}>{locationState==="requesting"||locationState==="checking"?"Checking…":"Use location"}</button></section>}
    {locationState==="granted"&&<div className="search-v3-status"><span/><strong>{loading?"Searching nearby…":`${sorted.length} active ${sorted.length===1?"Ping":"Pings"}`}</strong><small>{debouncedQuery?`matching “${debouncedQuery}”`:"available to discover"}</small></div>}
    <main className="search-v3-results">{error&&<div className="search-v3-empty"><h2>Search couldn’t load</h2><p>{error}</p></div>}{!error&&coordinates&&!loading&&sorted.length===0&&<div className="search-v3-empty"><PingIcon name="lostFound" size={28}/><h2>{debouncedQuery?"No nearby matches":"Quiet around here"}</h2><p>{debouncedQuery?"Try fewer words, another category, or a wider radius.":"There are no active Pings in this area right now."}</p></div>}{!error&&sorted.map(item=>{const meta=CATEGORY_DEFINITIONS[item.category];return <article key={item.id} className="search-v3-card" onClick={()=>openPing(item)} role="button" tabIndex={0} onKeyDown={e=>{if(e.key==="Enter")openPing(item);}}><div className="search-v3-cardtop"><span><PingIcon name={meta.icon} size={14}/>{meta.label}</span><b>{ageLabel(item.created_at)}</b></div><h2>{item.title}</h2><p>{item.body}</p>{item.mediaUrl&&<img src={item.mediaUrl} alt={`Photo attached to ${item.title}`} loading="lazy"/>}<div className="search-v3-meta"><span><PingIcon name="location" size={13}/>{(item.distance_meters/1609.344).toFixed(1)} mi</span><span><PingIcon name="confirmations" size={13}/>{item.confirmation_count} confirmed{item.lastConfirmedAt?` · ${confirmLabel(item.lastConfirmedAt)}`:""}</span><span><PingIcon name="replies" size={13}/>{item.comment_count}</span></div></article>;})}</main>
  </div><style jsx global>{`.search-v3-page{min-height:100vh;background:var(--ping-canvas);display:flex;justify-content:center;padding:20px}.search-v3-shell{width:min(100%,430px);min-height:calc(100vh - 40px);padding-bottom:28px}.search-v3-head{display:grid;grid-template-columns:42px 1fr 42px;gap:10px;align-items:center;padding:12px 0}.search-v3-head>div{text-align:center}.search-v3-head p{margin:6px 0 0;color:var(--ping-muted);font-size:9px}.search-v3-head>button{width:40px;height:40px;border:1px solid var(--ping-line);border-radius:13px;background:#fff;color:var(--ping-ink);display:grid;place-items:center;font-size:24px}.search-v3-intro{padding:18px 4px 8px}.search-v3-intro h1{font-size:29px;line-height:1.05;letter-spacing:-1px;margin:0 0 8px}.search-v3-intro p{margin:0;color:var(--ping-muted);font-size:12px;line-height:1.5}.search-v3-searchbox{margin:13px 0 11px;background:#fff;border:1px solid var(--ping-line);border-radius:15px;height:52px;display:grid;grid-template-columns:38px 1fr 38px;align-items:center;padding:0 8px}.search-v3-searchbox>svg{margin:auto;color:var(--ping-muted)}.search-v3-searchbox input{border:0;outline:0;background:transparent;width:100%;font-size:14px;color:var(--ping-ink)}.search-v3-searchbox button{border:0;background:var(--ping-surface-soft);border-radius:50%;width:28px;height:28px;font-size:18px}.search-v3-chips{display:flex;gap:7px;overflow:auto;padding:0 0 12px;scrollbar-width:none}.search-v3-chips::-webkit-scrollbar{display:none}.search-v3-chips button{flex:0 0 auto;min-height:34px;display:flex;align-items:center;gap:6px;border:1px solid var(--ping-line);background:#fff;border-radius:999px;padding:0 11px;color:var(--ping-muted);font-size:9px;font-weight:720}.search-v3-chips button.active{background:var(--ping-ink);color:#fff;border-color:var(--ping-ink)}.search-v3-controls{display:grid;grid-template-columns:1fr 1fr;gap:9px;padding-bottom:13px}.search-v3-controls label{background:var(--ping-surface-soft);border-radius:12px;padding:9px 10px;display:grid;grid-template-columns:1fr auto;align-items:center}.search-v3-controls span{font-size:8px;color:var(--ping-muted);font-weight:800;text-transform:uppercase}.search-v3-controls select{border:0;background:transparent;color:var(--ping-ink-2);font-size:10px;font-weight:750}.search-v3-location{padding:13px;border:1px solid rgba(60,131,246,.15);border-radius:15px;background:rgba(60,131,246,.04);display:grid;grid-template-columns:38px 1fr auto;gap:10px;align-items:center}.search-v3-location>span{width:36px;height:36px;display:grid;place-items:center;border-radius:10px;background:#fff;color:var(--ping-blue)}.search-v3-location strong{font-size:11px}.search-v3-location p{margin:3px 0 0;color:var(--ping-muted);font-size:9px;line-height:1.4}.search-v3-location button{border:0;background:var(--ping-ink);color:#fff;border-radius:10px;padding:9px 11px;font-size:9px;font-weight:750}.search-v3-status{padding:10px 3px 12px;display:flex;align-items:center;gap:7px;color:var(--ping-ink-2)}.search-v3-status>span{width:7px;height:7px;border-radius:50%;background:var(--ping-accent)}.search-v3-status strong{font-size:10px}.search-v3-status small{font-size:8px;color:var(--ping-muted);margin-left:auto}.search-v3-results{display:grid;gap:10px}.search-v3-card{background:#fff;border:1px solid var(--ping-line);border-radius:17px;padding:15px;cursor:pointer}.search-v3-cardtop{display:flex;justify-content:space-between;align-items:center}.search-v3-cardtop span{display:inline-flex;align-items:center;gap:6px;font-size:9px;font-weight:750;color:var(--ping-ink-2)}.search-v3-cardtop b{color:var(--ping-muted-2);font-size:8px}.search-v3-card h2{font-size:17px;line-height:1.2;margin:11px 0 7px}.search-v3-card>p{font-size:11px;line-height:1.5;color:var(--ping-muted);margin:0 0 11px;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.search-v3-card>img{width:100%;max-height:220px;object-fit:cover;border-radius:14px;margin:1px 0 11px}.search-v3-meta{display:flex;flex-wrap:wrap;gap:8px 12px;padding-top:10px;border-top:1px solid var(--ping-line);color:var(--ping-muted);font-size:8px}.search-v3-meta span{display:flex;align-items:center;gap:4px}.search-v3-empty{text-align:center;background:#fff;border:1px solid var(--ping-line);border-radius:17px;padding:30px 24px}.search-v3-empty svg{color:var(--ping-muted)}.search-v3-empty h2{font-size:17px;margin:8px 0 5px}.search-v3-empty p{font-size:11px;color:var(--ping-muted);line-height:1.5;margin:0}@media(max-width:520px){.search-v3-page{padding:0 16px}.search-v3-shell{width:100%;min-height:100vh}}`}</style></div>;
}
